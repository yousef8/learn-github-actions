name: Second Workflow

on:
  workflow_run:
    workflows: [first]
    branches: [master]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy
        run: echo "Deploying the project..."
